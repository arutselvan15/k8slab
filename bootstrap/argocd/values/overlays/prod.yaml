# Production-oriented defaults (TLS via ingress/cert-manager in Day 2).
# On Kind-only prod profile, add ingress or use port-forward with care.

configs:
  params:
    server.insecure: false

controller:
  replicas: 2

repoServer:
  replicas: 2

redis:
  enabled: true
