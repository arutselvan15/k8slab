# Shared Argo CD Helm values (all environments).
# Environment-specific settings: values/overlays/<profile>.yaml

global:
  domain: argocd.local

server:
  service:
    type: ClusterIP

controller:
  replicas: 1

repoServer:
  replicas: 1

applicationSet:
  replicas: 1

redis:
  enabled: true
