# Kind dev: local CA for argocd.dev (browser will warn until CA is trusted).

apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: selfsigned
  annotations:
    argocd.argoproj.io/sync-wave: "21"
spec:
  selfSigned: {}
